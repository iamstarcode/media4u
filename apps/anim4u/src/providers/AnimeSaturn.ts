import _ from 'lodash';

import { BaseProvider } from './BaseProvider.js';
import { homedir } from 'os';
import path from 'path';
/* 
export class AnimeSaturn extends BaseProvider {
  constructor({ options, query, provider }: IBaseProvider) {
    super({
      options,
      query,
      provider,
    });
  }
}
 */
